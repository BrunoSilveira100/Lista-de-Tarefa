# Lista-de-Tarefa

Criar uma aplicação simples de lista de tarefas usando HTML, CSS e JavaScript puro.

— um campo de texto + botão que insere um novo item na lista via createElement e appendChild
2 -  — ao clicar na tarefa, ela ganha um estilo riscado (line-through), usando classList.toggle
3 -  — cada item tem um botão "✕" que remove o elemento com removeChild ou remove()
4 -  — impedir que tarefas vazias sejam adicionadas, exibindo uma mensagem de erro no DOM

Desafio extra:
Salvar as tarefas no localStorage para persistir após recarregar a página
Filtrar tarefas por status:
